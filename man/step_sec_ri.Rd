% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step_sec_ri.R
\name{step_sec_ri}
\alias{step_sec_ri}
\title{RI Detector Processing for SEC}
\usage{
step_sec_ri(
  recipe,
  measures = NULL,
  dn_dc = NULL,
  dn_dc_column = NULL,
  instrument_constant = 1,
  output_col = NULL,
  role = NA,
  trained = FALSE,
  skip = FALSE,
  id = recipes::rand_id("sec_ri")
)
}
\arguments{
\item{recipe}{A recipe object.}

\item{measures}{Character vector of RI detector column names to process.
If \code{NULL}, will look for columns containing "ri" in the name.}

\item{dn_dc}{Refractive index increment (mL/g). Can be:
\itemize{
\item A single numeric value applied to all samples
\item \code{NULL} to skip dn/dc normalization (signal remains in detector units)
}}

\item{dn_dc_column}{Character name of a column containing sample-specific
dn/dc values. Overrides \code{dn_dc} if provided.}

\item{instrument_constant}{RI detector instrument constant. Default is 1.0
(no adjustment). This converts raw detector response to refractive index
units.}

\item{output_col}{Name for the output column. Default is to modify in place.}

\item{role}{Role for generated columns.}

\item{trained}{Logical indicating if the step has been trained.}

\item{skip}{Logical. Should the step be skipped when baking?}

\item{id}{Unique step identifier.}
}
\value{
An updated recipe with the new step added.
}
\description{
\code{step_sec_ri()} creates a \emph{specification} of a recipe step that processes
refractive index (RI) detector signals for SEC analysis, including
application of dn/dc (refractive index increment) values.
}
\details{
The refractive index (RI) detector is the most common concentration detector
in SEC/GPC. The detector response is proportional to the product of
concentration and dn/dc:

\deqn{RI_{signal} = K \times c \times (dn/dc)}

where:
\itemize{
\item K is the instrument constant
\item c is the concentration (mg/mL)
\item dn/dc is the refractive index increment (mL/g)
}

\strong{Common dn/dc values (in water at 25°C, 633 nm):}
\itemize{
\item Polystyrene in THF: 0.185 mL/g
\item PMMA in THF: 0.084 mL/g
\item PEG in water: 0.135 mL/g
\item Proteins: ~0.185 mL/g
\item DNA: ~0.170 mL/g
}
}
\examples{
\dontrun{
library(recipes)
library(measure)

# Apply fixed dn/dc value
rec <- recipe(~., data = sec_data) |>
  step_measure_input_long(ri_signal, location = vars(elution_time), col_name = "ri") |>
  step_sec_ri(dn_dc = 0.185) |>
  prep()

# Use sample-specific dn/dc from a column
rec <- recipe(~., data = sec_data) |>
  step_measure_input_long(ri_signal, location = vars(elution_time), col_name = "ri") |>
  step_sec_ri(dn_dc_column = "dn_dc") |>
  prep()
}
}
\seealso{
Other sec-chromatography: 
\code{\link{step_sec_baseline}()},
\code{\link{step_sec_concentration}()},
\code{\link{step_sec_detector_delay}()},
\code{\link{step_sec_mw_averages}()},
\code{\link{step_sec_mw_distribution}()},
\code{\link{step_sec_mw_fractions}()},
\code{\link{step_sec_uv}()}
}
\concept{sec-chromatography}
