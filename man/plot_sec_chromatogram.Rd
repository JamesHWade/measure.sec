% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-functions.R
\name{plot_sec_chromatogram}
\alias{plot_sec_chromatogram}
\title{Plot SEC Chromatogram}
\usage{
plot_sec_chromatogram(
  data,
  measures = NULL,
  sample_id = NULL,
  x_label = "Elution Time (min)",
  y_label = "Signal",
  normalize = FALSE,
  facet_by = c("none", "measure", "sample"),
  color_by = c("sample", "measure"),
  ...
)
}
\arguments{
\item{data}{A data frame containing SEC results with measure columns,
or a tibble from \code{\link[=measure_sec_slice_table]{measure_sec_slice_table()}}.}

\item{measures}{Character vector of measure column names to plot.
If \code{NULL}, plots all measure columns found.}

\item{sample_id}{Column name containing sample identifiers. If \code{NULL},
attempts to auto-detect or uses row numbers.}

\item{x_label}{Label for x-axis. Default is "Elution Time (min)".}

\item{y_label}{Label for y-axis. Default is "Signal".}

\item{normalize}{Logical. Normalize signals to 0-1 range for comparison?
Default is \code{FALSE}.}

\item{facet_by}{How to facet the plot. One of:
\itemize{
\item \code{"none"}: All on single plot (default)
\item \code{"measure"}: Separate panel per detector/measure
\item \code{"sample"}: Separate panel per sample
}}

\item{color_by}{What to map to color aesthetic. One of \code{"sample"} (default)
or \code{"measure"}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:geom_path]{ggplot2::geom_line()}}.}
}
\value{
A ggplot2 object.
}
\description{
Creates a chromatogram plot from SEC data showing detector signal vs
elution time/volume.
}
\details{
This is the fundamental SEC visualization showing raw or processed
chromatographic data. Works with both:
\itemize{
\item Processed recipe output (data frames with measure_list columns)
\item Slice tables from \code{\link[=measure_sec_slice_table]{measure_sec_slice_table()}}
}
}
\examples{
\dontrun{
library(recipes)
library(measure)

# Process SEC data
processed <- recipe(~., data = sec_triple_detect) |>
  step_measure_input_long(
    ri_signal,
    location = vars(elution_time),
    col_name = "ri"
  ) |>
  step_sec_baseline(measures = "ri") |>
  prep() |>
  bake(new_data = NULL)

# Basic chromatogram
plot_sec_chromatogram(processed, measures = "ri")

# Normalized overlay of multiple samples
plot_sec_chromatogram(processed, measures = "ri", normalize = TRUE)

# Faceted by sample
plot_sec_chromatogram(processed, facet_by = "sample")
}
}
\seealso{
Other sec-visualization: 
\code{\link{plot_sec}()},
\code{\link{plot_sec_calibration}()},
\code{\link{plot_sec_composition}()},
\code{\link{plot_sec_conformation}()},
\code{\link{plot_sec_multidetector}()},
\code{\link{plot_sec_mwd}()}
}
\concept{sec-visualization}
