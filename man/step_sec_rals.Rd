% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step_sec_rals.R
\name{step_sec_rals}
\alias{step_sec_rals}
\title{Right-Angle Light Scattering Processing for SEC}
\usage{
step_sec_rals(
  recipe,
  measures = NULL,
  concentration_col = NULL,
  angle = 90,
  laser_wavelength = 670,
  dn_dc = NULL,
  solvent_ri = 1.333,
  optical_constant = NULL,
  calibration_constant = NULL,
  output_mw = "mw_rals",
  min_signal = 0.01,
  role = NA,
  trained = FALSE,
  skip = FALSE,
  id = recipes::rand_id("sec_rals")
)
}
\arguments{
\item{recipe}{A recipe object.}

\item{measures}{Character vector of RALS measure columns. If \code{NULL}, the
step searches for measure columns containing "rals".}

\item{concentration_col}{Name of the concentration measure column (from
\code{step_sec_concentration()} or similar).}

\item{angle}{Detection angle in degrees. Default is 90.}

\item{laser_wavelength}{Laser wavelength in nm.}

\item{dn_dc}{Refractive index increment (mL/g). Required unless
\code{optical_constant} is provided.}

\item{solvent_ri}{Solvent refractive index. Default is 1.333 (water).}

\item{optical_constant}{Optional optical constant K; overrides dn/dc.}

\item{calibration_constant}{RALS instrument calibration constant. If \code{NULL},
results are in relative units.}

\item{output_mw}{Name for the molecular weight output column.}

\item{min_signal}{Minimum signal threshold (as fraction of max) below which
MW is set to NA.}

\item{role}{Role for generated columns.}

\item{trained}{Logical indicating if the step has been trained.}

\item{skip}{Logical. Should the step be skipped when baking?}

\item{id}{Unique step identifier.}
}
\value{
An updated recipe with the new step added.
}
\description{
\code{step_sec_rals()} creates a \emph{specification} of a recipe step that processes
right-angle light scattering (RALS) signals for absolute molecular weight.
}
\details{
RALS provides absolute MW using a 90-degree detector. It is most accurate
for smaller molecules where angular dependence is minimal (Rg << lambda/20).
}
\examples{
\dontrun{
library(recipes)
library(measure)

rec <- recipe(~., data = sec_triple_detect) |>
  step_measure_input_long(ri_signal, location = vars(elution_time), col_name = "ri") |>
  step_measure_input_long(rals_signal, location = vars(elution_time), col_name = "rals") |>
  step_sec_concentration(detector = "ri", injection_mass = 0.2, flow_rate = 1.0) |>
  step_sec_rals(measures = "rals", concentration_col = "ri", dn_dc = 0.185) |>
  prep()
}
}
\seealso{
Other sec-detectors: 
\code{\link{step_sec_concentration}()},
\code{\link{step_sec_dad}()},
\code{\link{step_sec_dls}()},
\code{\link{step_sec_intrinsic_visc}()},
\code{\link{step_sec_lals}()},
\code{\link{step_sec_mals}()},
\code{\link{step_sec_ri}()},
\code{\link{step_sec_uv}()},
\code{\link{step_sec_viscometer}()}
}
\concept{sec-detectors}
