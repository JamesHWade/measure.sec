% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polymer-analysis.R
\name{measure_sec_column_performance}
\alias{measure_sec_column_performance}
\title{Column Performance Metrics for SEC}
\usage{
measure_sec_column_performance(
  calibration_data,
  column_length = 30,
  column_diameter = 7.8,
  flow_rate = NULL,
  particle_size = NULL,
  dead_volume = NULL
)
}
\arguments{
\item{calibration_data}{A data frame containing calibration data with columns:
\itemize{
\item \code{retention} or \code{retention_time}: Retention time/volume
\item \code{mw} or \code{molecular_weight}: Molecular weight
\item \code{width}: Peak width (optional, for plate calculation)
}}

\item{column_length}{Column length in cm. Default is 30 cm.}

\item{column_diameter}{Column inner diameter in mm. Default is 7.8 mm.}

\item{flow_rate}{Flow rate in mL/min (optional, for linear velocity).}

\item{particle_size}{Particle size in micrometers (optional, for reduced HETP).}

\item{dead_volume}{Column dead volume in mL (optional).}
}
\value{
A list of class \code{sec_column_performance} containing:
\describe{
\item{separation_range}{MW range (exclusion limit to total permeation)}
\item{selectivity}{Slope of log(MW) vs retention (mL^-1)}
\item{hetp}{Height equivalent to theoretical plate (mm)}
\item{plates_per_meter}{Theoretical plates per meter}
\item{reduced_hetp}{HETP/particle_size (if particle_size provided)}
\item{peak_capacity}{Estimated peak capacity in separation range}
\item{resolution_factor}{Resolution per decade of MW}
}
}
\description{
Calculates comprehensive column performance metrics including HETP,
separation range, and efficiency parameters.
}
\details{
\strong{Key SEC Column Performance Metrics:}

\strong{HETP (Height Equivalent to Theoretical Plate):}
\deqn{HETP = \frac{L}{N}}

\strong{Reduced HETP (h):}
\deqn{h = \frac{HETP}{d_p}}
Optimal h ~ 2-3 for well-packed columns.

\strong{Selectivity (D):}
\deqn{D = \frac{d \log M}{dV_R}}
Higher D means better MW resolution.

\strong{Peak Capacity:}
\deqn{n_c = 1 + \frac{\sqrt{N}}{4} \ln\left(\frac{V_{exclusion}}{V_{total}}\right)}

\strong{Typical Performance Guidelines:}
\itemize{
\item HETP: < 50 um for analytical columns
\item Reduced HETP: 2-5 for good columns
\item Plates/meter: > 20,000 for HPLC SEC
}
}
\examples{
# From calibration standards
cal_data <- data.frame(
  retention = c(5.2, 6.1, 7.0, 8.2, 9.5, 10.8),
  mw = c(1200000, 400000, 100000, 30000, 5000, 580),
  width = c(0.4, 0.35, 0.30, 0.28, 0.25, 0.30)
)

perf <- measure_sec_column_performance(
  cal_data,
  column_length = 30,
  particle_size = 5
)
print(perf)
}
\seealso{
Other sec-qc: 
\code{\link{measure_sec_asymmetry}()},
\code{\link{measure_sec_plate_count}()},
\code{\link{measure_sec_recovery}()},
\code{\link{measure_sec_resolution}()},
\code{\link{measure_sec_suitability}()}
}
\concept{sec-qc}
