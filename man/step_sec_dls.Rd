% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step_sec_dls.R
\name{step_sec_dls}
\alias{step_sec_dls}
\title{Dynamic Light Scattering Processing for SEC}
\usage{
step_sec_dls(
  recipe,
  measures = NULL,
  temperature = 25,
  viscosity = NULL,
  laser_wavelength = 633,
  angle = 90,
  solvent_ri = 1.333,
  rg_col = "rg",
  output_cols = c("rh", "diffusion_coef"),
  role = NA,
  trained = FALSE,
  skip = FALSE,
  id = recipes::rand_id("sec_dls")
)
}
\arguments{
\item{recipe}{A recipe object.}

\item{measures}{Character vector of DLS measure columns. If \code{NULL}, the
step searches for measure columns containing "dls" or "corr".}

\item{temperature}{Temperature in degrees Celsius.}

\item{viscosity}{Solvent viscosity in mPa*s. If \code{NULL}, uses a water
approximation based on temperature.}

\item{laser_wavelength}{Laser wavelength in nm. Default is 633.}

\item{angle}{Scattering angle in degrees. Default is 90.}

\item{solvent_ri}{Solvent refractive index. Default is 1.333 (water).}

\item{rg_col}{Optional Rg measure column from MALS for Rg/Rh ratio.}

\item{output_cols}{Output column names for Rh and diffusion coefficient.}

\item{role}{Role for generated columns.}

\item{trained}{Logical indicating if the step has been trained.}

\item{skip}{Logical. Should the step be skipped when baking?}

\item{id}{Unique step identifier.}
}
\value{
An updated recipe with the new step added.
}
\description{
\code{step_sec_dls()} creates a \emph{specification} of a recipe step that processes
dynamic light scattering (DLS) correlation data to estimate diffusion
coefficient and hydrodynamic radius (Rh).
}
\details{
The DLS correlation function g2(tau) is approximated as:
\deqn{g2(\tau) = 1 + \beta e^{-2 \Gamma \tau}}
where Gamma is the decay rate. The diffusion coefficient is calculated as
D = Gamma / q^2 with q determined from the scattering angle and wavelength.
Rh is then obtained from the Stokes-Einstein equation.
}
\examples{
\dontrun{
library(recipes)
library(measure)

rec <- recipe(~., data = sec_triple_detect) |>
  step_measure_input_long(dls_corr, location = vars(lag_time), col_name = "dls") |>
  step_sec_dls(measures = "dls", temperature = 25, viscosity = 0.89) |>
  prep()
}
}
\seealso{
Other sec-detectors: 
\code{\link{step_sec_concentration}()},
\code{\link{step_sec_dad}()},
\code{\link{step_sec_intrinsic_visc}()},
\code{\link{step_sec_lals}()},
\code{\link{step_sec_mals}()},
\code{\link{step_sec_rals}()},
\code{\link{step_sec_ri}()},
\code{\link{step_sec_uv}()},
\code{\link{step_sec_viscometer}()}
}
\concept{sec-detectors}
