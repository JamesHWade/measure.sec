% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-functions.R
\name{sec_results}
\alias{sec_results}
\title{Create SEC Results Object}
\usage{
sec_results(data, sample_id = NULL)
}
\arguments{
\item{data}{A data frame containing processed SEC results with measure columns.
Typically the output from \code{bake()} on a prepped SEC recipe.}

\item{sample_id}{Optional. Column name containing sample identifiers.
If \code{NULL}, auto-detection is attempted.}
}
\value{
An object of class \code{sec_results} (inherits from \code{tbl_df}).
}
\description{
Constructor for the \code{sec_results} class, which wraps processed SEC/GPC data
and enables ggplot2's \code{autoplot()} functionality for automatic visualization.
}
\details{
The \code{sec_results} class provides a unified interface for SEC/GPC data that
enables:
\itemize{
\item Automatic plot selection via \code{autoplot()}
\item Integration with ggplot2 theming
\item Summary statistics access
}

\strong{Expected Data Structure:}
The input data should contain measure columns (list columns with \code{location}
and \code{value} components). Common measure columns include:
\itemize{
\item \code{ri}, \code{uv}, \code{mals} - Detector signals
\item \code{mw} - Molecular weight from calibration
\item \code{concentration} - Concentration profile
\item \code{intrinsic_visc} - Intrinsic viscosity
\item \code{rg} - Radius of gyration
}
}
\examples{
\dontrun{
library(recipes)
library(measure)
library(ggplot2)

# Process SEC data
processed <- recipe(~., data = sec_triple_detect) |>
  step_measure_input_long(ri_signal, location = vars(elution_time), col_name = "ri") |>
  step_sec_baseline(measures = "ri") |>
  step_sec_conventional_cal(standards = ps_standards) |>
  prep() |>
  bake(new_data = NULL)

# Wrap as sec_results
results <- sec_results(processed, sample_id = "sample_id")

# Use autoplot for automatic visualization
autoplot(results)
autoplot(results, type = "mwd")
autoplot(results, type = "chromatogram", normalize = TRUE)
}
}
\seealso{
Other sec-visualization: 
\code{\link{autoplot.sec_results}()},
\code{\link{plot_sec}()},
\code{\link{plot_sec_calibration}()},
\code{\link{plot_sec_chromatogram}()},
\code{\link{plot_sec_composition}()},
\code{\link{plot_sec_conformation}()},
\code{\link{plot_sec_multidetector}()},
\code{\link{plot_sec_mwd}()}
}
\concept{sec-visualization}
