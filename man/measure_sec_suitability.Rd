% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc-functions.R
\name{measure_sec_suitability}
\alias{measure_sec_suitability}
\title{System Suitability Test for SEC}
\usage{
measure_sec_suitability(
  data = NULL,
  peaks,
  reference_peaks = NULL,
  injected_mass = NULL,
  criteria = NULL,
  column_length = NULL
)
}
\arguments{
\item{data}{A data frame or tibble containing chromatogram data with at
minimum retention times and peak parameters.}

\item{peaks}{A data frame with peak information. Must contain columns:
\itemize{
\item \code{retention}: Peak retention time
\item \code{width}: Peak width (at half height unless specified)
\item \code{area}: Peak area (for recovery calculation)
\item \code{height}: Peak height (optional, for asymmetry from raw data
}}

\item{reference_peaks}{Character vector of peak names to use for resolution
calculation (e.g., c("dimer", "monomer")).}

\item{injected_mass}{Injected mass for recovery calculation (optional).}

\item{criteria}{A list of acceptance criteria. Default uses common
biopharmaceutical criteria.}

\item{column_length}{Column length in cm (for plates per meter calculation).}
}
\value{
A list of class \code{sec_suitability} containing:
\describe{
\item{results}{Data frame of calculated metrics and pass/fail status}
\item{passed}{Logical indicating if all criteria passed}
\item{summary}{Character summary of results}
\item{criteria}{Criteria used for evaluation}
}
}
\description{
Performs comprehensive system suitability testing for SEC analysis,
evaluating resolution, plate count, asymmetry, and other quality metrics.
}
\details{
System suitability testing (SST) verifies that the chromatographic system
is performing adequately before, during, and after sample analysis.

\strong{Standard SEC SST Parameters:}
\itemize{
\item Resolution: Rs >= 1.5 between critical pair
\item Plate count: N >= specified minimum
\item Tailing factor: 0.8 <= Tf <= 1.5
\item Mass recovery: 95-105\%
\item Retention time RSD: <= 1.0\%
\item Peak area RSD: <= 2.0\%
}

\strong{Regulatory References:}
\itemize{
\item USP <621> Chromatography
\item ICH Q2(R1) Validation
\item EP 2.2.46 Chromatographic Separation Techniques
}
}
\examples{
\dontrun{
# Define peaks from integration results
peaks <- data.frame(
  name = c("aggregate", "dimer", "monomer", "fragment"),
  retention = c(7.2, 8.5, 9.8, 11.5),
  width = c(0.3, 0.25, 0.28, 0.35),
  area = c(2.1, 5.3, 89.2, 3.4)
)

# Run system suitability
sst <- measure_sec_suitability(
  peaks = peaks,
  reference_peaks = c("dimer", "monomer"),
  injected_mass = 0.200,
  column_length = 30
)

print(sst)
}
}
\seealso{
Other sec-qc: 
\code{\link{measure_sec_asymmetry}()},
\code{\link{measure_sec_plate_count}()},
\code{\link{measure_sec_recovery}()},
\code{\link{measure_sec_resolution}()}
}
\concept{sec-qc}
