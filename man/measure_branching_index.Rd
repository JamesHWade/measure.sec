% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polymer-analysis.R
\name{measure_branching_index}
\alias{measure_branching_index}
\title{Calculate Branching Index}
\usage{
measure_branching_index(
  mw,
  rg = NULL,
  intrinsic_visc = NULL,
  reference = "linear",
  mh_linear = NULL,
  rg_linear_fit = NULL,
  method = c("g", "g_prime", "both")
)
}
\arguments{
\item{mw}{Numeric vector of molecular weights for branched samples.}

\item{rg}{Numeric vector of radius of gyration values (for g ratio).}

\item{intrinsic_visc}{Numeric vector of intrinsic viscosity values (for g').}

\item{reference}{Type of reference: either \code{"linear"} for comparison to
linear polymer theory, or a data frame containing linear reference data.}

\item{mh_linear}{Mark-Houwink parameters for linear reference polymer
(list with K and a). Required if \code{reference = "linear"} and using g'.}

\item{rg_linear_fit}{Rg-MW relationship for linear polymer: either a model
or a list with \code{slope} and \code{intercept} for log(Rg) = intercept + slope * log(M).}

\item{method}{Branching index type:
\itemize{
\item \code{"g"}: Radius of gyration ratio: g = Rg^2(branched) / Rg^2(linear)
\item \code{"g_prime"}: Viscosity ratio: g' = [eta](branched) / [eta](linear)
\item \code{"both"}: Calculate both g and g'
}}
}
\value{
A data frame with columns:
\describe{
\item{mw}{Molecular weight}
\item{g}{Branching index from Rg (if calculated)}
\item{g_prime}{Branching index from viscosity (if calculated)}
\item{branches_per_molecule}{Estimated branch points (Zimm-Stockmayer)}
}
}
\description{
Calculates the branching index (g or g') for branched polymers by comparing
their properties to linear reference polymers of the same molecular weight.
}
\details{
Branching reduces the hydrodynamic size of polymers compared to their linear
counterparts. This is quantified by the branching ratios:

\strong{Rg-based branching index (g):}
\deqn{g = \frac{R_g^2(branched)}{R_g^2(linear)}}

\strong{Viscosity-based branching index (g'):}
\deqn{g' = \frac{[\eta](branched)}{[\eta](linear)}}

The relationship between g and g' depends on polymer architecture:
\deqn{g' = g^\epsilon}

where epsilon ~ 0.5-1.5 depending on branching type.

\strong{Zimm-Stockmayer Model (random branching):}
\deqn{g = \frac{6}{n_b} \left[ \frac{1}{2} + \frac{(2 + n_b)^{1/2} - 1 - n_b/2}{n_b} \right]}

where n_b is the number of branch points per molecule.
}
\examples{
# Calculate branching index from Rg data
mw <- c(100000, 200000, 500000)
rg_branched <- c(12, 18, 32)
rg_linear <- c(15, 22, 40)  # Reference linear polymer

g <- measure_branching_index(
  mw = mw,
  rg = rg_branched,
  reference = data.frame(mw = mw, rg = rg_linear),
  method = "g"
)
}
\seealso{
Other sec-polymer: 
\code{\link{measure_conformation_data}()},
\code{\link{measure_mh_parameters}()}
}
\concept{sec-polymer}
