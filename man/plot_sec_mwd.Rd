% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-functions.R
\name{plot_sec_mwd}
\alias{plot_sec_mwd}
\title{Plot Molecular Weight Distribution}
\usage{
plot_sec_mwd(
  data,
  mw_col = "mw",
  concentration_col = NULL,
  sample_id = NULL,
  type = c("differential", "cumulative", "both"),
  show_averages = TRUE,
  log_mw = TRUE,
  x_label = NULL,
  y_label = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame containing SEC results with calibrated MW data.}

\item{mw_col}{Name of the molecular weight measure column. Default is \code{"mw"}.}

\item{concentration_col}{Name of the concentration/signal column used for
weighting. If \code{NULL}, uses the first available measure column.}

\item{sample_id}{Column name containing sample identifiers.}

\item{type}{Type of distribution to plot:
\itemize{
\item \code{"differential"}: dW/d(log M) vs log M (default)
\item \code{"cumulative"}: Cumulative weight fraction vs log M
\item \code{"both"}: Both on faceted plot
}}

\item{show_averages}{Logical. Show vertical lines for Mn, Mw, Mz?
Default is \code{TRUE}. Requires these columns to be present in data.}

\item{log_mw}{Logical. Use log10(MW) on x-axis? Default is \code{TRUE}.}

\item{x_label}{Label for x-axis. Default is auto-generated based on \code{log_mw}.}

\item{y_label}{Label for y-axis. Default is auto-generated based on \code{type}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:geom_path]{ggplot2::geom_line()}}.}
}
\value{
A ggplot2 object.
}
\description{
Creates a molecular weight distribution (MWD) plot showing differential
or cumulative distribution.
}
\details{
The MWD plot is the standard way to visualize polymer molecular weight
distributions. The differential distribution (dW/d log M) shows the
weight fraction of polymer at each molecular weight.

When \code{show_averages = TRUE}, vertical dashed lines are added for:
\itemize{
\item Mn (number-average): leftmost
\item Mw (weight-average): middle
\item Mz (z-average): rightmost
}
}
\examples{
\dontrun{
# After SEC processing with calibration
plot_sec_mwd(processed_data)

# Cumulative distribution
plot_sec_mwd(processed_data, type = "cumulative")

# Without MW average lines
plot_sec_mwd(processed_data, show_averages = FALSE)
}
}
\seealso{
Other sec-visualization: 
\code{\link{plot_sec}()},
\code{\link{plot_sec_calibration}()},
\code{\link{plot_sec_chromatogram}()},
\code{\link{plot_sec_composition}()},
\code{\link{plot_sec_conformation}()},
\code{\link{plot_sec_multidetector}()}
}
\concept{sec-visualization}
