% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-functions.R
\name{autoplot.sec_results}
\alias{autoplot.sec_results}
\title{Automatic Plot for SEC Results}
\usage{
\method{autoplot}{sec_results}(
  object,
  type = c("auto", "chromatogram", "mwd", "conformation", "composition"),
  overlay_mw = TRUE,
  detectors = c("ri", "uv", "mals"),
  log_scale = c("x", "none", "y", "both"),
  ...
)
}
\arguments{
\item{object}{An \code{sec_results} object created by \code{\link[=sec_results]{sec_results()}}.}

\item{type}{Type of plot to create. One of:
\itemize{
\item \code{"auto"}: Automatically detect best plot type (default)
\item \code{"chromatogram"}: Basic chromatogram (signal vs time)
\item \code{"mwd"}: Molecular weight distribution
\item \code{"conformation"}: Rg-MW or eta-MW scaling plot
\item \code{"composition"}: UV/RI ratio or composition plot
}}

\item{overlay_mw}{Logical. For chromatogram plots, overlay molecular weight
on secondary y-axis? Default is \code{TRUE} when MW data is available.}

\item{detectors}{Character vector of detector columns to plot for
multi-detector overlays. Default is \code{c("ri", "uv", "mals")}.}

\item{log_scale}{Character. Apply log scale to axes. Options:
\itemize{
\item \code{"x"}: Log scale on x-axis (default for MWD plots)
\item \code{"y"}: Log scale on y-axis
\item \code{"both"}: Log scale on both axes
\item \code{"none"}: No log scaling
}}

\item{...}{Additional arguments passed to the underlying plot function.}
}
\value{
A ggplot2 object.
}
\description{
Creates a ggplot2 visualization appropriate for SEC/GPC analysis results.
Automatically selects the best plot type based on available data, or
allows explicit selection via the \code{type} argument.
}
\details{
When \code{type = "auto"} (default), the plot type is selected based on
available data:
\enumerate{
\item If \code{mw} column present: MWD plot
\item If multiple detectors: Multi-detector overlay
\item Otherwise: Basic chromatogram
}

The resulting ggplot2 object can be further customized with standard
ggplot2 functions like \code{+ theme_bw()} or \code{+ labs()}.
}
\examples{
\dontrun{
library(ggplot2)

# Create sec_results object
results <- sec_results(processed_sec_data)

# Auto-detect best plot type
autoplot(results)

# Specific plot types
autoplot(results, type = "chromatogram")
autoplot(results, type = "mwd", show_averages = TRUE)
autoplot(results, type = "conformation")

# Customize with ggplot2
autoplot(results, type = "mwd") +
  theme_classic() +
  labs(title = "Molecular Weight Distribution")
}
}
\seealso{
Other sec-visualization: 
\code{\link{plot_sec}()},
\code{\link{plot_sec_calibration}()},
\code{\link{plot_sec_chromatogram}()},
\code{\link{plot_sec_conformation}()},
\code{\link{plot_sec_multidetector}()},
\code{\link{plot_sec_mwd}()},
\code{\link{sec_results}()}
}
\concept{sec-visualization}
