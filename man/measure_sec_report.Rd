% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-functions.R
\name{measure_sec_report}
\alias{measure_sec_report}
\title{Generate SEC Analysis Report}
\usage{
measure_sec_report(
  data,
  template = c("standard", "detailed", "qc"),
  output_format = c("html", "pdf", "docx"),
  output_file = NULL,
  title = NULL,
  author = "",
  sample_id = NULL,
  include_plots = TRUE,
  include_slice_table = FALSE,
  specs = NULL,
  open = interactive(),
  quiet = FALSE
)
}
\arguments{
\item{data}{A data frame containing SEC results with measure columns,
typically from a baked recipe.}

\item{template}{Report template to use: \code{"standard"} (default), \code{"detailed"},
or \code{"qc"}. See Details.}

\item{output_format}{Output format: \code{"html"} (default), \code{"pdf"}, or \code{"docx"}.}

\item{output_file}{Path for the output file. If \code{NULL} (default), creates
a file in the current working directory with an auto-generated name.}

\item{title}{Report title. Default varies by template.}

\item{author}{Report author. Default is empty.}

\item{sample_id}{Column name containing sample identifiers.}

\item{include_plots}{Logical. Include visualizations? Default is \code{TRUE}.}

\item{include_slice_table}{Logical. Include slice-by-slice data table?

Default is \code{FALSE}. Only used with \code{"detailed"} template.}

\item{specs}{Named list of QC specifications for the \code{"qc"} template.
See Details.}

\item{open}{Logical. Open the report after generation? Default is \code{TRUE}
for interactive sessions.}

\item{quiet}{Logical. Suppress Quarto output messages? Default is \code{FALSE}.}
}
\value{
Invisibly returns the path to the generated report file.
}
\description{
Creates a comprehensive report from SEC analysis results using Quarto
templates. Reports can be generated in HTML, PDF, or Word format.
}
\details{
\subsection{Templates}{

Three report templates are available:

\strong{\code{"standard"}} (default):
\itemize{
\item Molecular weight summary table
\item Chromatogram overlay
\item Molecular weight distribution plot
}

\strong{\code{"detailed"}}:
\itemize{
\item All content from standard template
\item Multi-detector overlay
\item Conformation plot (if MALS data available)
\item Optional slice data table
\item Analysis metadata
}

\strong{\code{"qc"}} (Quality Control):
\itemize{
\item System suitability metrics with pass/fail status
\item Plate count, asymmetry, and resolution tests
\item Calibration verification
\item Acceptance criteria summary
}
}

\subsection{QC Specifications}{

For the \code{"qc"} template, you can provide custom specifications:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{specs = list(
  plate_count_min = 10000,
  asymmetry_min = 0.8,
  asymmetry_max = 1.5,
  resolution_min = 1.5,
  recovery_min = 95,
  recovery_max = 105
)
}\if{html}{\out{</div>}}
}

\subsection{Requirements}{
\itemize{
\item Quarto must be installed on your system
\item The \code{quarto} R package must be installed
\item For PDF output, a LaTeX distribution is required
}
}
}
\examples{
\dontrun{
library(recipes)
library(measure)

# Process SEC data
processed <- recipe(~., data = sec_triple_detect) |>
  step_measure_input_long(ri, location = vars(time), col_name = "ri") |>
  step_sec_baseline() |>
  step_sec_conventional_cal(standards = ps_standards) |>
  step_sec_mw_averages() |>
  prep() |>
  bake(new_data = NULL)

# Generate standard HTML report
measure_sec_report(processed)

# Generate detailed PDF report
measure_sec_report(
  processed,
  template = "detailed",
  output_format = "pdf",
  title = "Polymer Characterization Results",
  author = "Lab Analyst"
)

# Generate QC report with custom specifications
measure_sec_report(
  sec_system_suitability,
  template = "qc",
  specs = list(plate_count_min = 15000)
)
}
}
\seealso{
Other sec-export: 
\code{\link{measure_sec_slice_table}()},
\code{\link{measure_sec_summary_table}()}
}
\concept{sec-export}
