% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-functions.R
\name{plot_sec_multidetector}
\alias{plot_sec_multidetector}
\title{Plot Multi-Detector SEC Overlay}
\usage{
plot_sec_multidetector(
  data,
  detectors,
  sample_id = NULL,
  samples = NULL,
  normalize = TRUE,
  x_label = "Elution Time (min)",
  facet = FALSE,
  ...
)
}
\arguments{
\item{data}{A data frame containing SEC results with multiple detector
measure columns.}

\item{detectors}{Character vector of detector column names to include.
Common values: \code{c("ri", "uv", "mals", "visc")}.}

\item{sample_id}{Column name containing sample identifiers. If \code{NULL},
plots all samples or auto-detects.}

\item{samples}{Character vector of specific sample IDs to plot.
If \code{NULL}, plots all samples.}

\item{normalize}{Logical. Normalize each detector to 0-1 range for
comparison? Default is \code{TRUE}.}

\item{x_label}{Label for x-axis. Default is "Elution Time (min)".}

\item{facet}{Logical. Create separate panel for each sample?
Default is \code{FALSE} (overlay).}

\item{...}{Additional arguments passed to \code{\link[ggplot2:geom_path]{ggplot2::geom_line()}}.}
}
\value{
A ggplot2 object.
}
\description{
Creates an overlay plot of multiple SEC detectors, optionally normalized
and aligned.
}
\details{
Multi-detector overlay plots are essential for:
\itemize{
\item Verifying detector alignment after delay correction
\item Identifying composition drift in copolymers (UV/RI differences)
\item Detecting aggregates (MALS response higher than expected from RI)
\item Checking for baseline issues across detectors
}

When \code{normalize = TRUE} (default), each detector signal is scaled to 0-1
range, making it easy to compare peak shapes and positions across
detectors with very different response magnitudes.
}
\examples{
\dontrun{
# Multi-detector overlay for single sample
plot_sec_multidetector(
  processed_data,
  detectors = c("ri", "uv", "mals"),
  samples = "PMMA-1"
)

# Faceted by sample
plot_sec_multidetector(
  processed_data,
  detectors = c("ri", "uv"),
  facet = TRUE
)
}
}
\seealso{
Other sec-visualization: 
\code{\link{plot_sec}()},
\code{\link{plot_sec_calibration}()},
\code{\link{plot_sec_chromatogram}()},
\code{\link{plot_sec_conformation}()},
\code{\link{plot_sec_mwd}()}
}
\concept{sec-visualization}
