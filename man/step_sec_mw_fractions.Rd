% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step_sec_mw_fractions.R
\name{step_sec_mw_fractions}
\alias{step_sec_mw_fractions}
\title{Calculate Molecular Weight Fractions for SEC/GPC}
\usage{
step_sec_mw_fractions(
  recipe,
  measures = NULL,
  cutoffs = c(1000, 10000, 1e+05),
  calibration = NULL,
  integration_range = NULL,
  prefix = "frac_",
  role = "predictor",
  trained = FALSE,
  skip = FALSE,
  id = recipes::rand_id("sec_mw_fractions")
)
}
\arguments{
\item{recipe}{A recipe object.}

\item{measures}{An optional character vector of measure column names.}

\item{cutoffs}{Numeric vector of MW cutoff values. For each cutoff, the step
calculates the weight fraction below and above that value.}

\item{calibration}{Calibration method for converting x-axis to log(MW).
See \code{\link[=step_sec_mw_averages]{step_sec_mw_averages()}} for details.}

\item{integration_range}{Optional numeric vector \code{c(min, max)} specifying
the x-axis range for integration. If \code{NULL}, uses full range.}

\item{prefix}{Prefix for output column names. Default is \code{"frac_"}.}

\item{role}{Role for generated columns. Default is \code{"predictor"}.}

\item{trained}{Logical indicating if the step has been trained.}

\item{skip}{Logical. Should the step be skipped when baking?}

\item{id}{Unique step identifier.}
}
\value{
An updated recipe with the new step added.
}
\description{
\code{step_sec_mw_fractions()} creates a \emph{specification} of a recipe step that
calculates weight fractions above and below specified molecular weight cutoffs.
}
\details{
For each cutoff value \code{C}, this step calculates:
\itemize{
\item \code{frac_below_C}: Weight fraction with MW < C
\item \code{frac_above_C}: Weight fraction with MW >= C
}

These fractions sum to 1.0 and are useful for characterizing polymer
distributions. Common cutoffs include:
\itemize{
\item 1000 Da for oligomer content
\item 10000 Da for low MW fraction
\item 100000 Da for high MW fraction
}
}
\examples{
\dontrun{
library(recipes)
library(measure)

# Calculate fractions at multiple cutoffs
rec <- recipe(~., data = sec_triple_detect) |>
  step_measure_input_wide(starts_with("signal_")) |>
  step_sec_baseline() |>
  step_sec_mw_fractions(cutoffs = c(1000, 10000, 100000)) |>
  prep()
}
}
\seealso{
Other sec-chromatography: 
\code{\link{step_sec_baseline}()},
\code{\link{step_sec_detector_delay}()},
\code{\link{step_sec_mw_averages}()},
\code{\link{step_sec_mw_distribution}()}
}
\concept{sec-chromatography}
